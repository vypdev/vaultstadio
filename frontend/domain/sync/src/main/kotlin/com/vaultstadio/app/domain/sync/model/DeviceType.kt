/**
 * Device type for sync registration.
 */

package com.vaultstadio.app.domain.sync.model

enum class DeviceType {
    DESKTOP_WINDOWS,
    DESKTOP_MAC,
    DESKTOP_LINUX,
    MOBILE_ANDROID,
    MOBILE_IOS,
    WEB,
    CLI,
    OTHER,
}
