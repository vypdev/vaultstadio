/**
 * Federation capability flags.
 */

package com.vaultstadio.app.domain.federation.model

enum class FederationCapability {
    RECEIVE_SHARES,
    SEND_SHARES,
    FEDERATED_IDENTITY,
    FEDERATED_SEARCH,
    ACTIVITY_STREAM,
    REAL_TIME_EVENTS,
}
